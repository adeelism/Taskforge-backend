# Database connection. If unset, the app falls back to a local SQLite file
# (sqlite:///./taskforge.db) so it runs with no setup.
# Use a real Postgres URL in any shared/production environment, and never
# commit real credentials.
DATABASE_URL=postgresql+psycopg2://postgres:CHANGE_ME@localhost:5432/taskforge
